#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tinyliquid/bin/tinyliquid" "$@"
  ret=$?
else 
  node  "$basedir/../tinyliquid/bin/tinyliquid" "$@"
  ret=$?
fi
exit $ret
